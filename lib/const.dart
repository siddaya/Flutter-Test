import 'package:flutter/material.dart';

const fBorderInputDecoration = OutlineInputBorder(
    borderSide: BorderSide(color: Colors.green),
    borderRadius: BorderRadius.all(Radius.circular(10)));

const eBorderInputDecoration = OutlineInputBorder(
    borderSide: BorderSide(color: Color.fromARGB(255, 8, 205, 240)),
    borderRadius: BorderRadius.all(Radius.circular(10)));

const kstyleRed = Color(0Xff903292);
